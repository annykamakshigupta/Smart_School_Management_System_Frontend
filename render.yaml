services:
  - type: web
    name: Smart-School-Management-System-Frontend
    runtime: node
    buildCommand: npm install && npm run build
    startCommand: npm run preview
    routes:
      - type: rewrite
        source: /*
        destination: /index.html